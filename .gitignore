# Secrets — never commit
.secrets/
*.pem
*-credentials.json
service-account*.json

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Node / build artifacts (in case we add them later)
node_modules/
dist/
.env
.env.local
